<Window x:Class="SeekbarTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="シークバー的タイムキーパー"
        Height="12"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Left="0">
    
    <Grid>
        <!-- シークバー背景 -->
        <Rectangle Name="BackgroundRect"
                   Fill="#80404040"
                   Height="8"
                   VerticalAlignment="Center"/>
        
        <!-- プログレス表示 -->
        <Rectangle Name="ProgressRect"
                   Fill="#80008000"
                   Height="8"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   Width="0"/>
        
        <!-- 右クリックメニュー -->
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="開始/停止" Click="ToggleTimer_Click"/>
                <MenuItem Header="リセット" Click="ResetTimer_Click"/>
                <Separator/>
                <MenuItem Header="時間設定..." Click="SetTime_Click"/>
                <Separator/>
                <MenuItem Header="終了" Click="Exit_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</Window>